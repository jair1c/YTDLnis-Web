{
  "buildCommand": "sh -c 'if [ -d \"frontend\" ]; then cd frontend && npm install && npm run build && mkdir -p ../dist && cp -r dist/* ../dist/; else npm run build; fi'",
  "outputDirectory": "dist"
}
